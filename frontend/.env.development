# Backend URL for the Vite dev server proxy.
# All /api/* requests from the frontend are forwarded to this address.
# Change this if your backend runs on a different port or host.
VITE_API_URL=https://localhost:7140
